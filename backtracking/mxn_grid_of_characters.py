class Solution:
    def dfs(self,board,word,i,j,k):
    #check if current coordinates are out of grid or the current cell doesn't match the current character of the work
    if board[i][j] !=word[k] or if not (0<=i<len(board))
    
   